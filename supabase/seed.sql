-- Supabase local-dev seed file. `supabase start` runs migrations first,
-- then applies this file. For this project, migration 002 and 003 already
-- contain all the reference data, so this file is intentionally empty —
-- its presence just signals to the CLI that seeding is handled.

-- If you want local-only fixture data (e.g. demo completed assessments for
-- investor rehearsal), add INSERT statements here. They will NOT run in
-- production (CI/CD applies migrations only, not seed.sql).

SELECT 1;
