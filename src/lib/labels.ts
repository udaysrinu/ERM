// Human-friendly labels for benchmark profile keys. Both Today and the
// command palette read these to render the active profile.
export const BENCHMARK_LABEL: Record<string, string> = {
  target: "Target",
  industry: "Industry",
  peer: "Peers",
  external: "External",
};
